package com.codebridge.featureflag.model;

/**
 * Enum representing the possible types of feature flag values.
 */
public enum ValueType {
    BOOLEAN,
    STRING,
    INTEGER,
    DOUBLE,
    JSON
}

