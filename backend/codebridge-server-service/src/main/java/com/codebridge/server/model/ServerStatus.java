package com.codebridge.server.model;

/**
 * Enum representing the possible states of a server.
 */
public enum ServerStatus {
    RUNNING,
    STOPPED,
    STARTING,
    STOPPING,
    TERMINATED,
    UNKNOWN
}

